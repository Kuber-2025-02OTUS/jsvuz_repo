apiVersion: v2
name: myapp
description: A Helm chart for deploying the Kubernetes application
type: application
version: 0.1.0
appVersion: "1.0"
dependencies:
  - name: redis
    version: "17.0.0"
    repository: "https://charts.bitnami.com/bitnami"
