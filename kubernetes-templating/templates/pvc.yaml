apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kuber-pvc
  namespace: homework
spec:
  storageClassName: "kuber-sc"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
